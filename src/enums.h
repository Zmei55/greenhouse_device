#pragma once

enum TimeApp: unsigned long {
    ONE_SECOND = 1000,
    ONE_MINUTE = ONE_SECOND * 60,
    FIFTEEN_MINUTE = ONE_MINUTE * 15,
    HALF_HOUR = ONE_MINUTE * 30,
    ONE_HOUR = ONE_MINUTE * 60,
};
